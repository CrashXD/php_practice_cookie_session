<?php
    session_start();

    // setcookie("city", "Omsk");
    // echo 'index';
    // echo time();
    /*
    if (!isset($_COOKIE['city'])) {
        setcookie("city", "Pavlodar", time() + 3600);
        $_COOKIE['city'] = "Pavlodar";
        echo 'Установили ' . $_COOKIE['city'];
    } else {
        echo 'Была установлена ' . $_COOKIE['city'];
    }
    */

    // setcookie("city", "Pavlodar", time() - 3600);
    // unset($_COOKIE['city']);

    // print_r($_COOKIE);



    // $_SESSION['city'] = 'New Yourk';
    // session_destroy();

    // print_r($_SESSION);